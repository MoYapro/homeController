<PreferenceScreen xmlns:app="http://schemas.android.com/apk/res-auto">

    <EditTextPreference
        app:key="prefIP"
        app:summary="Set the ip of your TV to make the app send commands to it"
        app:title="TV ip address (e.g. 192.168.1.111)" />

    <EditTextPreference
        app:key="prefPassword"
        app:summary="The TV needs to have a password (PSK) set and the same password has to be entered here"
        app:title="Password" />

</PreferenceScreen>